<Window x:Class="PlayerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PlayerWPF" 
        Title="Media Player" Height="550" Width="800">
    <Window.DataContext>
        <local:MediaPlayerViewModel />
    </Window.DataContext>

    <Grid>
        
        <StackPanel>
        <MediaElement x:Name="mediaElement" Source="{Binding MediaPath}" Width="600" Height="400" />
        <Button Content="Selecionar Mídia" Click="SelectMediaButton_Click" />
        <Button Content="Play" Click="PlayButton_Click" />
        <Button Content="Pause" Click="PauseButton_Click" />
        <Button Content="Parar" Click="StopButton_Click" />
        
        </StackPanel>

    </Grid>
</Window>
